import React from 'react';
import { Text, View } from 'react-native';

const RestrictedArea = () => (
    <View>
        <Text> Está é a área de area restrita </Text>
    </View>
);
export default RestrictedArea;