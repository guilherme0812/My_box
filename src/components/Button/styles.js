import { StyleSheet } from "react-native";

const styles = StyleSheet.create({  
    paddingButton: {
        padding: '3%',
        backgroundColor: '#333',
        marginBottom: 10
    },
    text: {
        color: '#fff'
    }
});
export default styles;